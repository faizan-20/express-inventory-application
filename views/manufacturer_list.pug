extends layout 

block content 
    h1= title 

    ul 
        each manufacturer in manufacturer_list 
            li 
                a(href= manufacturer.url) #{manufacturer.name}
                |   (est: #{manufacturer.est})
        
        else 
            li There are no Manufacturers